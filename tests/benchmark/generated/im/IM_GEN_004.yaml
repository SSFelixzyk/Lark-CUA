id: IM_GEN_004
level: L2
title: 给联系人发Hello消息
task: 使用Ctrl+K唤起「全局搜索框」，输入<<TEST_CONTACT>>进入对应单聊界面；点击「消息输入框」输入内容“Hello”，按下Enter键发送消息。
ui_hints: 「全局搜索框」位于会话列表区顶部，Ctrl+K可直接激活；「消息输入框」位于聊天内容区底部中间位置，输入后按Enter发送。
preconditions:
- 飞书桌面客户端已登录并处于前台
- <<TEST_CONTACT>>已存在于联系人列表中
success_criteria: 与<<TEST_CONTACT>>的单聊记录最底部显示内容为Hello的已发送消息气泡
checkpoints:
- 按下Ctrl+K后全局搜索框成功唤起并处于可输入状态
- 搜索结果列表中出现<<TEST_CONTACT>>条目
- 成功进入与<<TEST_CONTACT>>的单聊界面，顶部显示对应联系人名称
- 聊天记录底部出现内容为“Hello”的消息气泡
cli_verifications:
- type: im_message
  expected_text: Hello
expected_steps: 4
timeout_steps: 8
tags:
- im
- send_message
- core
generated: true
